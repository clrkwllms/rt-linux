
for N in $(git-branch | grep -vE ' master$| base$|for-linus' | cut -c3-); do

  ITEMS=$(git-rev-list --reverse base..$N)
  mkdir -p tip/$N

  for M in $ITEMS; do
    echo "$N: $M"
    git-show --pretty=email --no-merges $M | cat
    # | tail -n +2 | sed 's/^Author: /From: /'

  done

done

